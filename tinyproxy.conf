User proxyuser
Group proxyuser

Port 8224
Listen 0.0.0.0
Timeout 600

# Access control inside the container. The Compose example publishes ports only
# on 127.0.0.1 by default; if you expose them on 0.0.0.0, restrict this rule or
# protect access with an external firewall.
Allow 0.0.0.0/0
